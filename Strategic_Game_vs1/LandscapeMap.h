#pragma once
#include <cstdlib>
#include <ctime>
#include <string>
#include <iostream>
#include "Landscape.h"


/*enum lands {ground, water, mountain};

class LandscapeMap
{

private:
	FieldObject** map;
	unsigned int width;
	unsigned int length;
public:
	LandscapeMap(int width_, int length_){
		width = width_;
		length = length_;
		map = new FieldObject * [width];
		for (int i = 0; i < width; i++)
		{
			map[i] = new Landscape [length];
		}
	}

	void fillMapRandomly();
	void fillMap(std::string stringMap);
	FieldObject* getLandscape(int X, int Y) {
		return &(map[Y][X]);
	}

};




*/